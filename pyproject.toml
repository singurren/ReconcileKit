[project]
name = "reconcilekit"
version = "0.1.0"
authors = [
    { name = "Singurren", email = "singurren@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
reconcilekit = "reconcilekit.cli:main"

[build-system]
requires = ["uv_build>=0.9.0,<0.10.0"]
build-backend = "uv_build"
